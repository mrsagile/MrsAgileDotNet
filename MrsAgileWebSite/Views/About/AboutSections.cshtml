@model AboutMessages

    <div class="container" style="margin-top:100px; display:grid; ">
        <div class="row align-content-stretch align-items-lg-stretch">
            @foreach (var message in Model.Messages)
            {
                <partial name="_AboutParts" model="@message" />
            }

        </div>
    </div>
